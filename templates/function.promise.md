# Function Layer Extract Template

这是一份拆分视图模板，只适合从单一 `System Promise` 中抽取功能层审阅材料。

系统的唯一治理源应当是 [system.promise.md](/Users/jinof/source/Promise/templates/system.promise.md)，而不是把功能层单独当成一份并列 Promise。

## 基本信息

- Promise 名称：
- 领域 / 模块：
- 版本：
- 状态：draft / active / deprecated
- Owner：

## 依赖的字段层

列出本功能层视图依赖的字段层语义来源与版本。

- 

## 目标

说明本 Promise 要定义哪些行为承诺。

## 非目标

说明哪些行为不在本 Promise 范围内。

## 行为清单

| 行为 | 触发条件 | 前置条件 | 读取集合 | 写入集合 | 成功结果 |
| --- | --- | --- | --- | --- | --- |
| | | | | | |

## 行为定义

对每个行为分别描述。

### 行为：`<ActionName>`

#### 触发条件

- 

#### 前置条件

- 

#### 读取边界

- 允许读取：
- 不允许读取：

#### 写入边界

- 允许写入：
- 不允许写入：

#### 处理结果

- 成功时：
- 失败时：

#### 幂等性 / 一致性

- 

#### 副作用

列出允许的副作用，例如发消息、记日志、写审计记录。

- 

#### 禁止行为

- 不允许绕过字段层的状态约束
- 不允许写入未声明字段
- 不允许通过缓存、标记位、临时状态引入隐含业务状态
- 不允许把“实现方便”当作新增行为边界的理由

## 冲突处理

当功能层与字段层冲突时：

- 以字段层为准
- 先修订 `System Promise` 的字段层，再修订功能层，再改代码

## 错误与拒绝条件

列出必须拒绝执行的情况。

- 
- 

## 给 AI 的实现约束

- 代码只能实现已声明行为。
- 所有读写必须落在声明边界内。
- 如果行为需要新增字段、状态或语义，必须回退到 `System Promise` 更新字段层承诺。
