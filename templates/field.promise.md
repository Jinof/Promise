# Field Layer Extract Template

这是一份拆分视图模板，只适合从单一 `System Promise` 中抽取字段层审阅材料。

系统的唯一治理源应当是 [system.promise.md](/Users/jinof/source/Promise/templates/system.promise.md)，而不是把字段层单独当成一份并列 Promise。

## 基本信息

- Promise 名称：
- 领域 / 模块：
- 版本：
- 状态：draft / active / deprecated
- Owner：

## 目标

说明这份字段层抽取视图想展示的系统真相范围。

## 非目标

明确这份 Promise 不负责什么，避免把功能设计或实现细节混进来。

## 核心对象

| 对象 | 说明 | 生命周期归属 |
| --- | --- | --- |
| | | |

## 字段定义

对每个核心对象分别描述。

### 对象：`<ObjectName>`

| 字段 | 类型 | 必填 | 默认值 | 可空 | 说明 |
| --- | --- | --- | --- | --- | --- |
| | | | | | |

### 字段语义

逐条说明关键字段的业务语义，而不是只写类型。

- `<field>`：
- `<field>`：

### 状态定义

如果对象存在状态机，必须显式列出允许状态。

| 状态 | 含义 | 是否终态 | 允许迁移到 |
| --- | --- | --- | --- |
| | | | |

### 不变量

列出在任何实现中都必须成立的真相。

- 
- 

### 派生字段

如果某些字段是计算结果而不是独立状态，必须说明其来源。

| 字段 | 来源 | 是否持久化 | 说明 |
| --- | --- | --- | --- |
| | | | |

### 读写边界

说明谁可以读、谁可以写、哪些字段只能系统维护。

| 字段 | 可读取方 | 可写入方 | 备注 |
| --- | --- | --- | --- |
| | | | |

### 禁止隐含状态

明确哪些状态或语义不允许只存在于代码里而不进入 Promise。

- 不允许：
- 不允许：

## 全局约束

列出跨对象的约束。

- 
- 

## 变更规则

说明什么情况下必须先更新 `System Promise` 的字段层。

- 新增字段时
- 新增状态时
- 修改字段语义时
- 修改状态迁移规则时

## 给 AI 的实现约束

- 不允许新增未声明字段。
- 不允许引入未声明状态。
- 不允许把字段语义埋在注释、魔法值或分支里。
- 如果实现需要新状态，先回到 `System Promise` 更新字段层承诺。
